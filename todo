---прелоадер как у приложения 
---selectPage 
---imageRemover
---BOX-SHADOW
---menu title как у контейнера - 125 строчка 
---variant heading

наполнить 3 категории по 5 страниц (с иконками) 
страница с максимальным количеством примеров блоков

??? common styles (heading block, response coefficient)

---single page 
swap
confirm ( модалка у которой кнопки возвращающие true/false)

переструктурировать firestore. Возможность копирования приложения и запуска с нуля
глупое приложение 



useEffect(() => {
    if(confirmAnswer) { 
        props.removeItem(props.data.id)
    }
}, [confirmAnswer])

const removeItem = () => {  
    setIsVisibleConfirm(true) 
};
 
 
const [isVisibleConfirm, setIsVisibleConfirm] = React.useState(false)       

<Confirm
    show={isVisibleConfirm}
    setShow={setIsVisibleConfirm}
    setAnswer={setConfirmAnswer}
    title={'Remove paragraph?'}
    text={"You can't cancel this action."}
    removeText={"remove"}
/>